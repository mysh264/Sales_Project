node_modules
.env
.next
dist
out
.DS_Store

# Build artifact: presentation deck mirrored from present/ by
# scripts/sync-presentation.sh (npm run prebuild). The canonical source
# is present/ at the repo root; this directory is regenerated on every
# build and should not be committed.
public/presentation/

.env*.local

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

*.tsbuildinfo
storage/
generated/
playwright-report/
test-results/

# local test helpers / minted session tokens (never commit)
tests/_live/

# Graphify AST/semantic cache (regenerable)
graphify-out/cache/
.tmp-verify/
